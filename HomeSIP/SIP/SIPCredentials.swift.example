import Foundation

/// Template: copia questo file come SIPCredentials.swift (non versionato)
/// e inserisci le credenziali reali dell'interno SIP da usare per i test.
enum SIPCredentials {
    static let username = "101"
    static let password = "CHANGE-ME"
    static let domain = "192.168.1.1"
}
