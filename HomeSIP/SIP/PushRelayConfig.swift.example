import Foundation

/// Template: copia questo file come PushRelayConfig.swift (non versionato)
/// e inserisci l'indirizzo reale del push relay e lo shared secret configurato
/// in REGISTER_SHARED_SECRET nel repo SIP-Phone-PushRelay.
enum PushRelayConfig {
    static let baseURL = "http://192.168.1.1:8080"
    static let sharedSecret = "CHANGE-ME"
}
